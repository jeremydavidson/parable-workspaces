name: Build
on:
  push:
    branches: [master]
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  # Validate Conventional Commits
  validateCommits:
    name: Validate Commits
    runs-on: ubuntu-latest
    steps:
      # Check out the current repository
      - name: Fetch Sources
        uses: actions/checkout@v6

      # Validate Conventional Commits
      - name: Validate Conventional Commits
        uses: wagoid/commitlint-github-action@v5

  # Run ESLint Check
  lint:
    name: Lint Check
    runs-on: ubuntu-latest
    steps:
      - name: Fetch Sources
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Install Dependencies
        run: npm ci

      - name: Run Lint Check
        run: npm run lint

  # Run Prettier Formatting Check
  prettier:
    name: Prettier Check
    runs-on: ubuntu-latest
    steps:
      - name: Fetch Sources
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Install Dependencies
        run: npm ci

      - name: Run Prettier Check
        run: npm run format:check

  # Run Dependency Audit Check
  audit:
    name: Audit Check
    runs-on: ubuntu-latest
    steps:
      - name: Fetch Sources
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Run Audit Check
        run: npm audit

  # Run Vitest unit tests and publish coverage reports
  test-unit:
    name: Unit Tests
    runs-on: ubuntu-latest
    steps:
      - name: Fetch Sources
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Install Dependencies
        run: npm ci

      - name: Run Unit Tests with Coverage
        run: npm run test:unit:coverage

      - name: Upload Unit Coverage
        uses: actions/upload-artifact@v7
        with:
          name: unit-coverage
          path: coverage/unit

  # Run Mocha e2e tests in a VS Code Extension Host
  test-e2e:
    name: E2E Tests
    runs-on: ubuntu-latest
    steps:
      - name: Fetch Sources
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Install Dependencies
        run: npm ci

      - name: Run E2E Tests with Coverage
        run: xvfb-run -a npm run test:e2e:coverage

      - name: Upload E2E Coverage
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: e2e-coverage
          path: coverage/e2e
          if-no-files-found: ignore

  # Build and package extension
  buildExtension:
    name: Build Extension
    needs: [validateCommits, lint, prettier, audit, test-unit, test-e2e]
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Fetch Sources
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Install Dependencies
        run: npm ci

      - name: Compile Extension
        run: npm run compile

      - name: Package VSIX
        run: npx @vscode/vsce package --no-yarn --out parable-workspaces.vsix

      - name: Upload VSIX Artifact
        if: github.event_name == 'push'
        uses: actions/upload-artifact@v7
        with:
          name: vscode-extension-vsix
          path: parable-workspaces.vsix
